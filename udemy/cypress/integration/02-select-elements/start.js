/// <reference types="cypress" />

it('visits todoMVC app', () => {

  cy
    .visit('localhost:3000');

  // select via tag
  // cy
  //   .get('input'); 

  // select via id

  // select via class

  // select via attribute

  // select via relation

});
